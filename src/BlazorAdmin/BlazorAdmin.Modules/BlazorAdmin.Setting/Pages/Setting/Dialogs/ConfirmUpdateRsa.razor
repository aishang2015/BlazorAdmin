<MudDialog DisableSidePadding="true">
	<DialogContent>
		<div style="width:600px;" class="px-6 py-2">
			<div class="d-flex align-center">
				<MudIcon Icon="@Icons.Material.Filled.Warning" Size="Size.Large" Class="mr-2"></MudIcon>
				<MudText Typo="Typo.h6">新的密钥会导致所有旧token失效，错误的密钥可能会导致系统崩溃，请确认后再进行操作。</MudText>
			</div>
			<div class="d-flex align-center mt-6">
				<MudSpacer />
				<MudButton Variant="Variant.Text" Color="Color.Error" Size="Size.Medium"
						   StartIcon="@Icons.Material.Filled.ConfirmationNumber" OnClick="Confirm">确认生成</MudButton>
			</div>
		</div>
	</DialogContent>
</MudDialog>